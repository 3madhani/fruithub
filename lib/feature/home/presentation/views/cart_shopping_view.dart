import 'package:flutter/material.dart';

class CartShoppingView extends StatelessWidget {
  const CartShoppingView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
